class NetworkConfiguration:
    def __init__(self, ip_range, total_machines):
        self.ip_range = ip_range
        self.total_machines = total_machines

# Add more configuration settings as needed
